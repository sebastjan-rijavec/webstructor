/**
 * Build-time globals injected via Vite's `define` config. Values are
 * substituted as literals into the bundle at build time.
 */

/** package.json version, baked in by vite.config.ts. */
declare const __APP_VERSION__: string;
